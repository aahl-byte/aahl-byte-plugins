{
  "name": "plan-clarity-template",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/validate.js && bash -c \"netstat -ano | grep LISTENING | grep :5173 > /dev/null 2>&1 && echo 'Server already running on port 5173' || vite\"",
    "build": "node scripts/validate.js && vite build",
    "preview": "node scripts/validate.js && bash -c \"netstat -ano | grep LISTENING | grep :5173 > /dev/null 2>&1 && echo 'Server already running on port 5173' || preview\""
  },
  "devDependencies": {
    "svelte": "^5.0.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0",
    "js-yaml": "^4.1.0"
  }
}
